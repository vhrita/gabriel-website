---
/**
 * IOSWaitlist.astro — captura de e-mail pro lançamento iOS (waitlist).
 *
 * Substitui a badge morta da App Store ("em breve"). Decisão do Vitor:
 * trocar badge inerte por lead-gen. Zero-JS: <details> nativo expande
 * um form Formspree (POST puro, sem AJAX — Formspree responde com sua
 * página de obrigado).
 *
 * Degradação graciosa: sem PUBLIC_FORMSPREE_ID, renderiza um botão
 * "iOS em breve" desabilitado (sem form quebrado apontando p/ /f/undefined).
 *
 * Visual: light v4. O trigger casa a altura/peso visual da StoreBadge md
 * (50px) pra ficar par com a badge do Google Play ao lado.
 *
 * Props:
 *   size?: 'md' | 'sm' — md (hero/CTA, 50px) · sm (footer, 36px)
 *   align?: 'start' | 'center' — alinhamento do painel expandido
 *   class?: string
 */

interface Props {
  size?: 'md' | 'sm';
  align?: 'start' | 'center';
  class?: string;
}

const { size = 'md', align = 'start', class: className } = Astro.props;

const formspreeId = import.meta.env.PUBLIC_FORMSPREE_ID;
const enabled = !!formspreeId;
const action = enabled ? `https://formspree.io/f/${formspreeId}` : undefined;

// id único pra ligar label/input/aria sem colidir entre instâncias (hero + CTA)
const uid = `ios-wl-${Math.random().toString(36).slice(2, 8)}`;
---

{enabled ? (
  <details class:list={['ios-wl', `ios-wl--${size}`, `ios-wl--${align}`, className]}>
    <summary class="ios-wl__trigger" aria-label="Ser avisado quando o app lançar no iOS">
      <svg class="ios-wl__apple" width="15" height="18" viewBox="0 0 384 512" fill="currentColor" aria-hidden="true">
        <path d="M318.7 268.7c-.2-36.7 16.4-64.4 50-84.8-18.8-26.9-47.2-41.7-84.7-44.6-35.5-2.8-74.3 20.7-88.5 20.7-15 0-49.4-19.7-76.4-19.7C63.3 141.2 4 184.8 4 273.5q0 39.3 14.4 81.2c12.8 36.7 59 126.7 107.2 125.2 25.2-.6 43-17.9 75.8-17.9 31.8 0 48.3 17.9 76.4 17.9 48.6-.7 90.4-82.5 102.6-119.3-65.2-30.7-61.7-90-61.7-91.9zm-56.6-164.2c27.3-32.4 24.8-61.9 24-72.5-24.1 1.4-52 16.4-67.9 34.9-17.5 19.8-27.8 44.3-25.6 71.9 26.1 2 49.9-11.4 69.5-34.3z"/>
      </svg>
      <span class="ios-wl__trigger-label">Avise-me no iOS</span>
    </summary>

    <div class="ios-wl__panel">
      <p class="ios-wl__hint">Deixe seu e-mail e avisamos assim que o Gabriel chegar no iPhone.</p>
      <form
        action={action}
        method="POST"
        class="ios-wl__form"
      >
        <input type="hidden" name="_subject" value="Gabriel — waitlist iOS" />
        <input type="hidden" name="origem" value="landing-page" />
        <label class="sr-only" for={uid}>Seu e-mail</label>
        <input
          id={uid}
          type="email"
          name="email"
          required
          autocomplete="email"
          placeholder="seu@email.com"
          class="ios-wl__input"
        />
        <button type="submit" class="ios-wl__submit">Avisar</button>
      </form>
    </div>
  </details>
) : (
  <span
    class:list={['ios-wl', 'ios-wl--disabled', `ios-wl--${size}`, className]}
    role="img"
    aria-label="App Store — Em breve"
  >
    <span class="ios-wl__trigger ios-wl__trigger--static">
      <svg class="ios-wl__apple" width="15" height="18" viewBox="0 0 384 512" fill="currentColor" aria-hidden="true">
        <path d="M318.7 268.7c-.2-36.7 16.4-64.4 50-84.8-18.8-26.9-47.2-41.7-84.7-44.6-35.5-2.8-74.3 20.7-88.5 20.7-15 0-49.4-19.7-76.4-19.7C63.3 141.2 4 184.8 4 273.5q0 39.3 14.4 81.2c12.8 36.7 59 126.7 107.2 125.2 25.2-.6 43-17.9 75.8-17.9 31.8 0 48.3 17.9 76.4 17.9 48.6-.7 90.4-82.5 102.6-119.3-65.2-30.7-61.7-90-61.7-91.9zm-56.6-164.2c27.3-32.4 24.8-61.9 24-72.5-24.1 1.4-52 16.4-67.9 34.9-17.5 19.8-27.8 44.3-25.6 71.9 26.1 2 49.9-11.4 69.5-34.3z"/>
      </svg>
      <span class="ios-wl__trigger-label">iOS em breve</span>
    </span>
  </span>
)}

<style>
  .ios-wl {
    display: inline-block;
  }

  .ios-wl--center {
    text-align: center;
  }

  /* Trigger — casa a altura da StoreBadge md (50px) */
  .ios-wl__trigger {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    height: 50px;
    padding-inline: 1.125rem;
    background: var(--color-text-strong);   /* navy — par visual c/ badge dark */
    color: #FFFFFF;
    border-radius: 8px;
    font-family: var(--font-family-sans);
    font-weight: 600;
    font-size: 0.9375rem;
    letter-spacing: 0.01em;
    cursor: pointer;
    list-style: none;
    user-select: none;
    white-space: nowrap;
    transition: background 200ms ease, transform 200ms ease, opacity 200ms ease;
    -webkit-font-smoothing: antialiased;
  }

  .ios-wl--sm .ios-wl__trigger {
    height: 36px;
    padding-inline: 0.875rem;
    font-size: 0.8125rem;
  }

  .ios-wl__trigger::-webkit-details-marker {
    display: none;
  }

  .ios-wl:not(.ios-wl--disabled) .ios-wl__trigger:hover {
    background: var(--color-celestial-700);
    transform: translateY(-1px);
  }

  .ios-wl:not(.ios-wl--disabled) .ios-wl__trigger:active {
    transform: translateY(0);
  }

  details.ios-wl > summary:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  .ios-wl__apple {
    flex-shrink: 0;
    opacity: 0.95;
  }

  /* Disabled state (sem Formspree configurado) */
  .ios-wl--disabled .ios-wl__trigger--static {
    background: var(--color-bg-soft);
    color: var(--color-text-muted);
    cursor: not-allowed;
  }

  /* Painel expandido */
  .ios-wl__panel {
    margin-top: 0.875rem;
    padding: 1rem 1.125rem;
    background: var(--color-bg-alt);
    border: 1px solid var(--color-divider);
    border-radius: 12px;
    max-width: 22rem;
    animation: ios-wl-in 220ms ease-out;
  }

  .ios-wl--center .ios-wl__panel {
    margin-inline: auto;
    text-align: left;
  }

  @keyframes ios-wl-in {
    from { opacity: 0; transform: translateY(-4px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .ios-wl__hint {
    font-family: var(--font-family-sans);
    font-size: 0.8125rem;
    line-height: 1.5;
    color: var(--color-text-body);
    margin: 0 0 0.75rem;
  }

  .ios-wl__form {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .ios-wl__input {
    flex: 1 1 11rem;
    min-width: 0;
    height: 42px;
    padding-inline: 0.875rem;
    font-family: var(--font-family-sans);
    font-size: 0.9375rem;
    color: var(--color-text-strong);
    background: #FFFFFF;
    border: 1px solid var(--color-divider);
    border-radius: 8px;
    transition: border-color 150ms ease, box-shadow 150ms ease;
  }

  .ios-wl__input::placeholder {
    color: var(--color-text-muted);
  }

  .ios-wl__input:focus-visible {
    outline: none;
    border-color: var(--color-accent-deep);
    box-shadow: 0 0 0 3px rgba(128, 107, 33, 0.15);
  }

  .ios-wl__submit {
    height: 42px;
    padding-inline: 1.25rem;
    background: var(--color-accent-deep);   /* gold-deep — AA em branco */
    color: #FFFFFF;
    border: none;
    border-radius: 8px;
    font-family: var(--font-family-sans);
    font-weight: 600;
    font-size: 0.9375rem;
    cursor: pointer;
    white-space: nowrap;
    transition: background 200ms ease, transform 200ms ease;
  }

  .ios-wl__submit:hover {
    background: var(--color-divine-800);
    transform: translateY(-1px);
  }

  .ios-wl__submit:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }
</style>
