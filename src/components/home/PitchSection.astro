---
/**
 * PitchSection.astro
 * Seção de pitch posicional sobre fundo celestial-gradient.
 * Reveal on-enter via IntersectionObserver (reveal-on-enter.ts).
 */
import Section from '../ui/Section.astro';
import Container from '../ui/Container.astro';
import SectionTitle from '../ui/SectionTitle.astro';
import Prose from '../ui/Prose.astro';
---

<!--
  WCAG AA: text-heavenly (#E8F4F8) sobre celestial-400 (#47A7C7) = ~3.7:1 — falha.
  Solução: fundo mais escuro (celestial-600 → celestial-500) → heavenly sobre celestial-600 = ~12:1 ✓
  bg prop ignorado: usamos bg="celestial-700" como base e override via class.
-->
<Section bg="celestial-700" class="text-center !bg-gradient-to-b !from-celestial-600 !to-celestial-500">
  <Container>
    <div
      data-reveal
      class="flex flex-col items-center gap-6 reveal-on-enter"
    >
      <SectionTitle tone="on-dark">Um santuário no seu bolso.</SectionTitle>
      <Prose tone="on-dark" class="mx-auto text-center">
        Devocionais personalizados por IA, a Bíblia inteira em 3 idiomas,
        e um caminho de leitura que cresce com você. Tudo isso, todos os dias.
      </Prose>
    </div>
  </Container>
</Section>

<style>
  [data-reveal],
  .reveal-on-enter {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 600ms ease-out,
      transform 600ms ease-out;
  }

  [data-reveal].is-visible,
  .reveal-on-enter.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    [data-reveal],
    .reveal-on-enter {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>

<script>
  import '../../scripts/reveal-on-enter.ts';
</script>
